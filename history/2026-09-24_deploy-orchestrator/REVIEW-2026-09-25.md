# Repository and live solution review — 25 September 2026

Reviewed branch `feat/deploy-orchestrator`, HEAD `9f20d8d`, using read-only Dataverse and Power Automate API calls. Verified Azure tenant and account before requests, and Dataverse WhoAmI plus systemuser identity in ADMIN, DEV and TEST: `kriall076@7xpydh.onmicrosoft.com`, tenant `1c5afb69-a82c-4c81-b2cc-743ce7f91dac`.

The workspace root holds design documentation; `DemoDeployPipeline` is the Git repository. Existing diagram modifications were left intact. No solution, flow, connection, SharePoint configuration, or deployment was changed. No commit was made.

## Findings, in priority order

1. **P1 — TEST Demo still has a placeholder Products list ID.** Live `dev_ProductsList` is `00000000-0000-0000-0000-000000000000`; `dev_SharePointSite` points to ALM-Test. The installed managed solution therefore lacks a usable configured Products list ID. Complete the TEST list setup and mapping, then verify app playback as a target user. This is consistent with the outstanding task in `HANDOFF-2026-09-24.md`; a successful solution import does not establish usable app data access. SharePoint list existence and app playback were not tested in this review.

2. **P2 — Deployment authentication enforces tenant but not account.** `pipeline/flowapi.py:12–17` checks only `tenantId` before acquiring the token. A different account in the same tenant can pass and perform deployment writes, contrary to the account-specific runbook. Validate the approved account as well as the tenant before token acquisition. This review used an additional exact-account check and independently verified live identity.

3. **P2 — Flow update lookup is not scoped to ALMPipeline.** `pipeline/deploy.py:59–69` selects the first workflow with a matching display name across ADMIN, then disables and overwrites it. An unrelated same-name workflow can be modified, and an existing out-of-solution workflow is not added to ALMPipeline. Resolve by solution membership and stable ID, and reject ambiguous matches. Current live C1–C3 membership is correct; this is a latent redeployment defect, not observed cross-solution damage.

4. **P2 — The repository entry point routes to a superseded checkpoint.** `README.md` directs readers to `docs/alm/HANDOFF.md` as authoritative. That checkpoint says bosso only, no Demo flow, no TEST deployment, and continuation at Task 5. The September 24 handoff and runbook describe the current kriall076 exception, deployed orchestrator, managed TEST solution, and completed acceptance tests. Update the entry point and mark the older checkpoint historical to prevent continuation with stale instructions.

## Live solution/component inventory

| Environment | Solution | Version/type | Verified components |
|---|---|---|---|
| ADMIN | ALMPipeline / ALM Pipeline | 1.0.0.0, unmanaged | 4 cloud flows, 2 connection references |
| DEV | Demo | 1.0.0.0, unmanaged | Demo Products canvas app, 2 variable definitions, 1 connection reference |
| TEST | Demo | 1.0.0.0, managed | Demo Products canvas app, 2 variable definitions, 1 connection reference |

ADMIN: `f2280ea5-6793-e664-8f21-ea3ba6a4cb5c`, organization `2a49ea7b-18b1-f111-8add-0022486f7441`, URL `https://adminorg774eae27.crm17.dynamics.com`.

ALMPipeline ID: `2d9c9cdb-a9b7-f111-aaae-000d3a832204`. Publisher: `almspike`, prefix `alm`.

| ADMIN component | State/binding | Dataverse component ID |
|---|---|---|
| ALM C1 - Deploy (parent) | Started / Activated | 48b8294a-aab7-f111-aaae-000d3a832204 |
| ALM C2 - Import (child) | Started / Activated | 0fb24d06-aab7-f111-aaae-000d3a832204 |
| ALM C3 - Post-import (child) | Started / Activated | 20b24d06-aab7-f111-aaae-000d3a832204 |
| ALM Setup - SharePoint config | Stopped / Draft | 61c5b1e1-a9b7-f111-aaae-000d3a832204 |
| alm_PipelineDataverse | shared-commondataser-88f9738e | 2f9c9cdb-a9b7-f111-aaae-000d3a832204 |
| alm_PipelineSharePoint | shared-sharepointonl-a0f00819 | 5ec5b1e1-a9b7-f111-aaae-000d3a832204 |

C1 orchestrates config/mapping prechecks, managed DEV export and ZIP archival, optional C2 import, optional C3 post-import, and run logging. C2 constructs import component parameters from configuration. C3 verifies connection bindings, updates existing variable value rows, and activates stopped solution flows. The setup helper is a separate administrative utility; it was inspected for membership/state, not compared against every possible generated command payload.

Both Demo solutions contain `dev_demoproducts_9414e` (Demo Products), `dev_SharePointSite`, `dev_ProductsList`, and `dev_SharePoint`. Neither contains a cloud flow.

| Configuration | DEV | TEST |
|---|---|---|
| SharePoint site current value | https://7xpydh.sharepoint.com/sites/ALM-Dev | https://7xpydh.sharepoint.com/sites/ALM-Test |
| Products list current value | 54358b50-7727-4a83-adc9-d76dbed307e3 | 00000000-0000-0000-0000-000000000000 |
| dev_SharePoint binding | 2a3c71eab7b44af482c2d87154edc8ee | shared-sharepointonl-0f567e53 |

Both variable definitions have null defaults. Actual connection credential health and user permissions were not established by reading connection-reference bindings.

Other visible ADMIN business solutions are Development, FlowAdminCore, FlowAdminMonitoring, and FlowAdminGovernance, all unmanaged 1.0.0.0 with publisher M365 / prefix ms365. They belong to the separate FlowError project per the current handoff; their internals were not audited. Standard/default and Microsoft-managed solutions were also inventoried. ALMSpike is absent from the visible solution list, consistent with the documented cleanup.

## Validation and remaining evidence gaps

- **59/59 tests passed**, using the installed Python 3.12 environment: `python -m pytest pipeline/tests -q`.
- **No C1–C3 definition drift:** freshly generated definitions, committed `pipeline/definitions/*.json`, and live Dataverse `clientdata.properties.definition` agree exactly. Live connection-reference definitions also agree exactly with generated definitions.
- Dataverse activation states agree with Power Automate Started/Stopped states.
- Latest C1 run `08584113783912272076327088956CU31` failed on 24 September at 05:42:23Z. Its log explicitly reports expected mapping `dev_SharePoint|shared-sharepointonl-WRONG0000`, matching the runbook's intentional wrong-binding test. Prechecks and export succeeded, import was skipped, and post-import failed. The preceding C1 run `08584113785786468893531670159CU09` succeeded. This historical log does not prove the current SharePoint mapping has been restored; current ALMConfig/ALMConnections rows were not read.
- C3 flow activation has no live coverage from Demo because there is no cloud flow in either Demo solution. C4 app sharing is not implemented; it remains documented future work.
- No deployment, app playback, SharePoint data operation, solution checker run, or permission/licensing audit was executed. Existing run history was read only.
- Connection succeeded through Azure-authenticated Web API calls. The separate PAC check was blocked by sandbox access to its local token-cache lock; Dataverse CLI has no profile and no Dataverse MCP is callable in this session. No MCP setup or CLI-cache repair is claimed.
